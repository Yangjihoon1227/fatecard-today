{
  "name": "fatecard-today-v25",
  "private": true,
  "version": "25.0.0",
  "type": "module",
  "scripts": {
    "dev": "node dev.cjs",
    "build": "tsc -b && vite build",
    "start": "node server.cjs",
    "preview": "vite preview --host 0.0.0.0",
    "deploy:build": "tsc -b && vite build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {},
  "description": "FateCard.today v25: premium home, single collapsible sidebar, full-deck spread draw, selectable card count."
}
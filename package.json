{
  "name": "fatecard-today-v38",
  "private": true,
  "version": "38.0.0",
  "type": "module",
  "scripts": {
    "dev": "node dev.cjs",
    "build": "tsc -b && vite build",
    "start": "node server.cjs",
    "preview": "vite preview --host 0.0.0.0",
    "deploy:build": "tsc -b && vite build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {},
  "description": "FateCard.today v38: pin npm in Docker to avoid Render npm 10.9.7 exit-handler build failure."
}